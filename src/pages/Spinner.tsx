import React from 'react';
import './CustomSpinner.css'; // Import your custom CSS

const CustomSpinner = () => {
    return (
        <div className="spinner-container">
            <div className="spinner"></div>
        </div>
    );
};

export default CustomSpinner;